<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Avalonia.UI.ViewModels"
             x:Class="Avalonia.UI.Pages.PluginManagementPage"
             x:DataType="viewModels:PluginManagementViewModel"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="600">
    <Grid Margin="16" RowDefinitions="Auto,Auto,*">
        
        <StackPanel Grid.Row="0" Spacing="4" Margin="0 0 0 16">
            <TextBlock Text="Plugin Management" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Install, uninstall, enable or disable plugins" FontSize="14" Opacity="0.6"/>
        </StackPanel>

        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Margin="0 0 0 12">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Install Plugin" Command="{Binding InstallPluginCommand}" IsEnabled="{Binding !IsInstalling}"/>
                <Button Content="Refresh" Command="{Binding RefreshPluginsCommand}"/>
            </StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" VerticalAlignment="Center" FontSize="13"/>
        </Grid>

        <ProgressBar Grid.Row="1" IsVisible="{Binding IsInstalling}" Value="{Binding InstallProgress}" 
                     Height="4" Margin="0 40 0 0" IsIndeterminate="False"/>

        <ScrollViewer Grid.Row="2">
            <StackPanel Spacing="8">
                <ItemsControl ItemsSource="{Binding Plugins}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="viewModels:PluginItemViewModel">
                            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0 0 0 4">
                                <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto,Auto">
                                    
                                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                                        <Border CornerRadius="4" Padding="6 2" Background="{Binding StateColor}">
                                            <TextBlock Text="{Binding StateText}" FontSize="11" Foreground="White"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                        <Button Content="Enable" Command="{Binding $parent[UserControl].DataContext.EnablePluginCommand}" 
                                                CommandParameter="{Binding}"
                                                IsVisible="{Binding State, Converter={x:Static viewModels:PluginStateConverters.IsNotLoaded}}"
                                                Classes="small"/>
                                        <Button Content="Disable" Command="{Binding $parent[UserControl].DataContext.DisablePluginCommand}" 
                                                CommandParameter="{Binding}"
                                                IsVisible="{Binding State, Converter={x:Static viewModels:PluginStateConverters.IsLoaded}}"
                                                Classes="small"/>
                                        <Button Content="Uninstall" Command="{Binding $parent[UserControl].DataContext.UninstallPluginCommand}" 
                                                CommandParameter="{Binding}"
                                                IsVisible="{Binding !IsBuiltIn}"
                                                Classes="small danger"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="16" Margin="0 4 0 0">
                                        <TextBlock FontSize="12" Opacity="0.6">
                                            <Run Text="Version: "/><Run Text="{Binding Version}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="12" Opacity="0.6">
                                            <Run Text="Author: "/><Run Text="{Binding Author}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Description}" 
                                               FontSize="13" Opacity="0.7" Margin="0 4 0 0"
                                               TextWrapping="Wrap" MaxHeight="40"/>

                                    <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding ErrorMessage}" 
                                               FontSize="12" Foreground="#F44336" Margin="0 4 0 0"
                                               IsVisible="{Binding ErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}"
                                               TextWrapping="Wrap"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border IsVisible="{Binding Plugins.Count, Converter={x:Static viewModels:PluginStateConverters.IsEmpty}}"
                        BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="32"
                        HorizontalAlignment="Center" Margin="0 40 0 0">
                    <StackPanel HorizontalAlignment="Center" Spacing="8">
                        <TextBlock Text="No plugins installed" FontSize="16" Opacity="0.5" HorizontalAlignment="Center"/>
                        <TextBlock Text="Click 'Install Plugin' to add a new plugin" FontSize="13" Opacity="0.4" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
